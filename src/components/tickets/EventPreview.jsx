import Video1 from '../../assets/home-vid-1.mp4';
import { useState } from 'react';

const EventPreview = () => {
  const [event, selectEvent] = useState(null);

  return <div className="">hello</div>;
};

export default EventPreview;
